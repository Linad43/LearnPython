from black.linegen import delimiter_split

# LearnPython

Проект для **изучения и практики Python**.  
В репозитории реализованы небольшие модули для обработки банковских транзакций, генерации данных и работы с масками номеров карт и счетов.

Проект создан в образовательных целях и постепенно дополняется новыми функциями и тестами.

---

# Содержание

- [Технологии](#технологии)
- [Функциональность](#функциональность)
- [Установка](#установка)
- [Использование](#использование)
- [Тестирование](#тестирование)
- [Разработка](#разработка)
- [To Do](#to-do)
- [Команда проекта](#команда-проекта)
- [Лицензия](#лицензия)
- [Источники](#источники)

---

# Технологии

В проекте используются:

- Python 3
- pytest
- typing (type hints)
- generators
- Git / GitHub

---

# Функциональность

Проект содержит несколько модулей.

## generators.py

Генераторы для работы с транзакциями.

### filter_by_currency

Фильтрует транзакции по валюте.

```python
filter_by_currency(transactions, "USD")
```

### transaction_descriptions

Возвращает генератор описаний транзакций.

```python
transaction_descriptions(transactions)
```

### card_number_generator

Генерирует номера банковских карт в формате:

```python
0000 0000 0000 0001
0000 0000 0000 0002
```

Пример:

```python
card_number_generator(1, 5)
```

## processing.py

Функции обработки списка транзакций.

```python
filter_by_state
```

Фильтрует транзакции по состоянию.

По умолчанию:

```
EXECUTED
```

Пример:

```python
filter_by_state(transactions, "EXECUTED")
```

### sort_by_date

Сортирует транзакции по дате.

```python
sort_by_date(transactions)
```

По умолчанию сортировка идет от новых к старым.

## decorators.py

Файл функций декораторов

### log

Декоратор логирования

```python
@log(filename)
```

По умолчанию параметр `filename` отсутствует, и тогда логирование идет прямо в консоль. При вводе имени файла, логирование будет в файл.

##external_api.py

Файл функций связи с интернетом

```python
convert_to_rub(transaction: Transaction) -> float
```

Функция возвращающая сумму операции в рублях, конвертируя валюту по текущему курсу

##until.py

Файл вспомогательных функций

```python
read_json(path) -> list[Transaction]
```

Функция возвращающая список операций. В качестве параметра принимает путь до файла  

##filepos.py

Файл вспомогательных функций-операций с файлами

```python
read_csv(path, delimiter) -> list[Transaction]
```

Функция возвращающая список операций. В качестве параметра принимает путь до csv файла и делитель, который по умолчанию является делителем по умолчанию для функции `csv.DictReader(delimiter=",")` 

```python
read_excel(path) -> list[Transaction]
```

Функция возвращающая список операций. В качестве параметра принимает путь до excel файла

## Установка

Клонируйте репозиторий:

```
git clone https://github.com/Linad43/LearnPython.git
```

Перейдите в папку проекта:

```
cd LearnPython
```

Использование

Запуск программы:

```
python main.py
```

## Тестирование

В проекте используются тесты pytest.

Установка pytest:

```
pip install pytest
```

Запуск тестов:

```
pytest
```

## Разработка

Для работы с проектом установите необходимые зависимости:

```
pip install -r requirements.txt
```

Рекомендуется использовать виртуальное окружение.

Пример:

```
python -m venv venv
source venv/bin/activate
pip install pytest
```

To Do
[x] Добавить README
[x] Реализовать генераторы
[x] Добавить тесты
[x] Добавить логирование
[ ] Улучшить структуру проекта
[ ] Добавить документацию функций
[ ] Добавить больше тестов

## Команда проекта

- linad43 linad43merzlikin@yandex.com - для связи с разработчиком
- 
## Лицензия:

Этот проект лицензирован по [лицензии MIT](LICENSE.md).

## Источники

[metanit.com](https://metanit.com/python/)
